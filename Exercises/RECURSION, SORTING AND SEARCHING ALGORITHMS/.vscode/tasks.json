{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/RECURSION, SORTING AND SEARCHING ALGORITHMS.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}